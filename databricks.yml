bundle:
  name: "databricks-genie-slackbot"

sync:
  include:
    - slackbot/app.yaml

variables:
  app_name:
    description: "Name of the Slackbot Databricks App"
    default: "databricks-genie-slackbot"

resources:
  apps:
    slackbot_app:
      name: "${var.app_name}"
      description: "Databricks Genie Slack Bot"
      source_code_path: "./slackbot"
